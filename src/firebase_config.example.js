import firebase from 'firebase';

// change this filename to firebase_config.js and add your own Firebase config object.

const config = {
  // Your Firebase Config options...
};

export default config;